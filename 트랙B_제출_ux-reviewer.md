# [에이전트 결과 공유] 구현 전 UX 사전검토 에이전트 ux-reviewer — 5만 토큰 읽고 25줄만 반환

## 실험 주제

컨텍스트를 가장 많이 소비하는 작업(코드 작성 전 UX 검토 — 여러 소스 파일을 가로질러 읽어야 함)을 서브에이전트로 분리하기. (트랙 B)

## 에이전트 이름과 역할

- **이름**: `ux-reviewer` — "만들기 전에, 안 만들 이유를 찾아주는 동료"
- **역할**: myzip(내집마련 시세 트래킹 서비스)의 UI/기능 변경을 **코드 작성 전에** UX 전문가 관점에서 사전 검토. 수억 원짜리 비가역 결정을 다루는 서비스 특성에 맞춰 "빠른 입력"보다 "틀린 판단을 막는 것"을 최우선 렌즈로 검토한다.
- **업로드 파일**: `ux-reviewer.md` (프로젝트의 .claude/agents/에 배치)
- **모델**: frontmatter에서 `model` 필드를 의도적으로 생략 — 세션의 최상위 모델(현재 Fable 5)을 항상 상속받아, 특정 모델에 고정되지 않고 판단 품질이 중요한 검토 작업에 늘 최신 상위 모델이 쓰이도록 설계

## 가설

1. UX 검토처럼 파일을 많이 읽는 작업을 격리된 서브에이전트로 분리하면, 메인 대화의 컨텍스트를 거의 쓰지 않고 검토 결과만 받을 수 있다.
2. 기능을 만들려는 쪽(메인 세션)이 스스로 검토하면 "만들어도 되는 이유"를 찾는 관성이 생기지만, 코드를 쓸 수 없는(Read/Grep/Glob만 가진) 별도 에이전트는 그 관성 없이 검토할 수 있다.

## 실행한 것

- 에이전트 정의 파일(`ux-reviewer.md`) 작성: 제품 맥락 + 검토 렌즈 5개(비가역성 → 판단 왜곡 → 입력 부담 → 정보구조 → 상시지침) + 25줄 이내 출력 형식 강제 + description에 호출/제외 조건 명시
- 통제 실험 1회: 실험용 가상 구상("랭킹 페이지에 가격대 필터 추가 — 예산 범위 밖 단지는 숨김")을 검토시키고 토큰·출력을 측정. 검토 케이스는 실제 개발 예정 기능이 아니며, 에이전트 프롬프트를 서브에이전트로 실행해 측정했다(격리 구조·도구 제한은 실제 운용과 동일).

## 결과 (2026-07-16 실측)

- **컨텍스트 격리**: 서브에이전트가 파일 5개(약 900줄)를 읽으며 토큰 49,898 소비, 도구 호출 7회 — 메인 대화에는 25줄 이내 판정만 반환됨. 가설 1 확인.
- **검토 품질**: 판정 "수정 후 진행" — ① 후보를 소리 없이 숨기면 시세 변동 때마다 랭킹에서 단지가 증발해 사용자가 원인을 알 수 없음 → 흐림 처리 + "예산 초과" 뱃지로 표시 유지 ② 시세 미입력 단지가 필터에 걸려 등록 직후 사라지는 문제 → 필터 대상에서 제외 ③ 실제 소스를 읽고 화면에 이미 있는 예산 입력 필드와의 중복을 찾아내 "새 UI를 만들지 말고 기존 필드를 확장하라"고 판정. 지정한 출력 형식(판정/막아야 할 것/고려할 것, 25줄 이내)도 준수됨.

## 배운 점

- **토큰 격리는 이 에이전트의 부차적 가치였다.** 검토 후 구현에 들어가면 메인 세션이 같은 파일을 다시 읽으므로 순절약이 아니다. 진짜 가치는 "새 UI 대신 기존 필드 확장" 같은 판정으로 **불필요한 구현 자체를 걸러낼 때** 나온다.
- **비개발자에게 코드 가시성을 붙여준다.** "이 변경이 점수 로직과 충돌하는가"는 소스를 읽어야만 알 수 있다. 이 에이전트는 UX 판단을 대신하는 게 아니라, 디자이너인 나에게 없는 코드 수준의 흐름 추적을 보태준다.
- **description이 곧 호출 정확도다.** "언제 부를 것(새 화면·플로우·정보구조 변경) / 언제 부르지 말 것(문구 수정·버그픽스)"을 description에 못 박으면 불필요한 호출로 인한 낭비를 막을 수 있다.

## 다음 액션

- 실전 워크플로우 투입: 이번 측정은 가상 케이스 통제 실험 1회이므로, 실제 기능 변경 시 검토를 거치게 해서 판정 품질과 호출 타이밍을 검증
- 판정 결과가 누적되면 "막아야 할 것" 지적의 적중률(실제로 문제였는가)을 되돌아보고 검토 렌즈를 조정

## 활용하실 분들께 — 재사용 방법

이 에이전트는 myzip 전용이지만 **골격은 어떤 프로젝트에도 재사용 가능**합니다. 파일 안의 "제품 맥락" 섹션(사용자·목적·스택·화면 경로)만 본인 프로젝트로 교체하면 됩니다. 나머지 — 검토 렌즈 구조, 25줄 이내 출력 강제, "사용자에게 실제로 일어나는 일로 서술" 규칙, description의 호출/제외 조건 명시 — 는 그대로 두는 것을 권합니다. 이 골격이 검토 품질과 컨텍스트 격리를 만드는 핵심입니다. 받은 파일은 프로젝트의 `.claude/agents/` 폴더에 넣으면 바로 동작합니다.
